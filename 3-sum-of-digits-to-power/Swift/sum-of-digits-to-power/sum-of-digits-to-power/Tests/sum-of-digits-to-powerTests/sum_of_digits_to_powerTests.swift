import XCTest
@testable import sum_of_digits_to_power

final class sum_of_digits_to_powerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(sum_of_digits_to_power().text, "Hello, World!")
    }
}
